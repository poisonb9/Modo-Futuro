# HANDOFF — 03/09/2026

Medido no fim da sessao, nao lembrado. Cada numero tem a fonte ao lado.

⚠️ **LEIA TAMBEM `PIPELINE.md`** na raiz do repo — o mapa de ponta a ponta,
escrito em 02/09 justamente porque uma sessao limpa gastou meia manha
confundindo as duas entradas de corte.

---

## 1. ONDE ISSO COMECOU, E O QUE ERA MENTIRA

A sessao abriu com "duas filas vazias por causa da cota do Gemini". A cota era
parte da verdade. Por baixo havia **seis defeitos**, e nenhum era visivel de
fora: o pipeline dizia `success`, o log dizia "0 ainda nao agendado", o vigia
dizia "Nada novo." — e nada disso era erro.

    token do .env morto ha' 3 dias     -> vigia mudo, 375 passadas, exit 0
    sonda contando lentidao como cota  -> teto 2 onde cabia 3
    contador punindo fonte boa         -> fonte perfeita quase expulsa
    upload com credencial errada       -> 4 runs, ~7h de runner perdidas
    clipe novo se auto-bloqueando      -> "0 agendaveis" com 10 vagas livres
    vigia sem dizer o canal            -> maquiagem elegivel no canal de chips

⚠️ **O padrao vale mais que a lista:** todos falhavam em SILENCIO, e varios
tinham guarda que errava pro lado "seguro" — que aqui significa desligar.

---

## 2. O QUE FOI CONSERTADO (tudo com teste, tudo em producao)

    ecc93a9  sonda: timeout 8s->20s, `mudo` fora do denominador
    d485c51  conta_saida separada de conta; a fila aprende `recorte`
    d69828a  KeyError que EU abri no orquestrador (1h15 de fila parada)
    7d5bcd6  clipe novo nao se auto-bloqueia; vigia manda o canal
    1f57633  marca `nao_publicar`
    f038ffd  veredito da cota decide pela SERIE; rede offline religada
    b08561a  motor RECUSA canal que nao e' dele

Suite: **49/49**.

⚠️ Um teste passa perto de 60s e reprova por TIMEOUT se o laco usar `timeout
60`. Use 90. Em 03/09 isso me deu um "1 falhou" que nao existia.

---

## 3. ⚠️ O QUE O DONO VIU, E QUE NENHUMA MAQUINA PEGOU

Ele abriu o proprio TikTok e achou o que a suite inteira nao achava: um clipe
do @cozinha.internacional dizendo, na legenda E na dublagem:

    "Em seguida, leve ao forno a 220°C Fahrenheit ou 220 Celsius."

Duas causas DIFERENTES, e eu confundi as duas antes de medir:

**(a) Este motor cortou receita.** Videos de cozinha cairam no RAW, o vigia
nao mandava canal, sairam como `modofuturo`, e ESTE motor nao converte
medidas — conversao e' o diferencial do motor da cozinha. Oito clipes assim.
→ Aposentados (`nao_publicar`), e `engine/escopo.py` impede que se repita.

**(b) O motor da COZINHA converte errado.** O clipe que ele viu era de LA'. O
lookahead `(?!\s*c)` excluia "Celsius" e nao "Fahrenheit" — ou seja, cobria o
caso raro e nao o comum. TODA receita com forno saia com a unidade orfa.

⚠️ **O conserto de (b) esta' PRONTO E TESTADO, e NAO foi enviado.**

    repo:    bryanaw2121-sketch/pipeline
    commits: 1aaa835 + 89808f5   (suite deles 18/18)
    bloqueio: "Permission to bryanaw2121-sketch/pipeline denied to poisonb9"

O patch esta' em `scratchpad/conserto_fahrenheit.patch` e foi entregue ao
Bryan. Aplicar com `git am`, ou dar Write a `poisonb9` em Settings >
Collaborators (⚠️ o convite vai pro e-mail da conta poisonb9, nao pro
principal — ele nao recebeu).

---

## 4. ESTADO AGORA (medido 21:36 UTC)

    @truque.importado   9  (+50h)     @modofuturo    8  (+46h)
    @atefalhar          8  (+46h)     @semanestesia  4  (+22h)
    @cozinha            3  (+18h)
    TOTAL 32 posts agendados — NENHUM canal vazio

    fila de cortes  18 pendentes · 14 prontas · 1 sem_fonte
    manifesto       122 clipes, ZERO sem canal, 14 aposentados

⚠️ **@modofuturo e @truque.importado NAO tem fonte pendente** — vivem do
estoque de prontas. Os 18 pendentes sao so' de semanestesia (15 janelas de
podcast) e atefalhar (3). Quando o estoque acabar, esses dois param, e a
ordem por folga NAO resolve: ela escolhe entre o que existe. Precisa de video
novo nas pastas `MODO FUTURO` e `Truque Importado` do RAW.

⚠️ **@cozinha nao e' servida por este motor**, por desenho. Quem repoe e' o
motor de la'.

---

## 5. O QUE ESTA' RODANDO SOZINHO

    cron cortar_fila     */30 (entrega de 1,8h a 4,8h — best-effort)
    encadeamento         cada corte que termina dispara o cortar_fila
    vigia do RAW         10 em 10 min, 1 por passada
    desempenho.yml       de hora em hora, agora TAMBEM alimenta o registro
                         offline de publicados (estava congelado ha' 5 dias)

⚠️ **OS MONITORES DESTA SESSAO MORREM COM ELA.** O disparo automatico por
cota era um monitor meu, nao infraestrutura. Sem ele, quem reacende a fila e'
so' o cron e o encadeamento — que bastam, mas com mais latencia.

---

## 6. PENDENTE DO BRYAN

  1. **Acesso Write** a `bryanaw2121-sketch/pipeline` (ou aplicar o patch);
  2. **Regenerar `GEMINI_API_KEY_2`** — deu 403 em TODAS as medicoes de 2 dias;
     e' chave morta ocupando lugar no rodizio;
  3. **Esvaziar a lixeira da conta principal** (~2,7 GB) — e' a conta que
     agora recebe os clipes;
  4. **Video novo no RAW** pras pastas MODO FUTURO e Truque Importado;
  5. Decidir se tira do ar o clipe do cookie com "220°C Fahrenheit" (esta'
     publicado, 173 views). ⚠️ Eu nao apago publicacao.

---

## 7. ⚠️ O QUE EU ERREI, PRA PROXIMA SESSAO NAO REPETIR

  - **Dei veredito de dia inteiro a partir de UMA foto de cota, TRES vezes.**
    16:19 "seca" -> 3/5 vinte minutos depois. 00:03 "esgotada, SEM
    AMBIGUIDADE" -> 4/5 vinte minutos depois. Nem `429` prova o dia: o Gemini
    usa 429 pro limite por minuto tambem. Hoje o `sondar_cota` exige a serie.
  - **Provei o caminho que eu controlava.** Medi o Nemotron na minha maquina,
    escrevi teste, declarei pronto — sem conferir se as chaves existiam como
    secret. Nao existiam.
  - **Empurrei um commit com a suite VERMELHA.** O laco de teste imprime o
    resultado mas o `&&` seguinte nao depende dele.
  - **Escrevi teste que casa com a GRAFIA** (nome de argumento, janela de N
    caracteres) em vez do comportamento. Reprovaram refactors corretos.
  - **Afirmei defeito no dedup sem conferir** — a inconsistencia era do meu
    script de medicao, nao do pipeline.
  - **Heredoc com regex quebra o escape.** Tres vezes. Para editar codigo com
    `\d` e `\s`, use a ferramenta de edicao, nao `python - <<'PY'`.
  - **Uma sabotagem de teste que nao sabotou nada** me deu 9 verdes falsos.
    Confirme que o arquivo MUDOU antes de acreditar no resultado.

---

## 8. O QUE NAO FAZER SEM ELE PEDIR

  - postar ou apagar clipe; lixeira nunca delete, e so' apos perguntar;
  - usar fonte PT como padrao (reserva premium; decisao suspensa);
  - montar o teste multi-voz ("calma");
  - mexer em processos do `bryan_fx_vision`;
  - escrever na raiz do repo times-report (mora la' a verificacao do TikTok).
