# Handoff — 28/08/2026

> Continuacao de `handoff_26-08-2026.md`. Onde houver conflito, **este manda**.
> Dia de duas frentes: a cadencia nova entrou em teste, e um segundo canal
> nasceu — em projeto **separado**, fora deste repositorio.

---

## 1. O teste de cadencia esta' em andamento

**5 posts/dia**, decisao do Bryan em 26/08 a' noite. Grade nova:
`08:15, 11:33, 16:27, 19:03, 20:50` — saiu o 13:07, o par mais apertado.

E entrou um **teto duro** `MAX_POR_DIA = 5` que conta os ENVIADOS, nao so' os
agendados. A grade sozinha nunca segurou o volume: 25/08 saiu com 8 posts e
26/08 com 11, ambos acima dos 6 slots que existiam. Post que dispara sai de
`scheduled` e o slot volta a parecer livre.

### O numero que ainda falta ler

| dia | posts | mediana |
|---|---|---|
| 26/08 | **11** | **523** |
| 27/08 | **5** | **?** |

Os posts de 27/08 passaram das 40h — a comparacao esta' pronta e **nao foi
rodada**. E' a resposta que motivou a mudanca de cadencia. `python historico.py`.

## 2. Correcao importante sobre a medicao

**O flag `confiavel` da falsa confianca em post novo.** Ele so' verifica se a
metrica foi atualizada ALGUMA VEZ depois da publicacao — nao se esta' atual.

Medido em 28/08 cruzando com print do TikTok:

| post | Buffer | TikTok real |
|---|---|---|
| Por que fabricar seus proprios chips | **76** (marcado MEDIDO) | **241** |
| Cultura de trabalho Taiwan | 0 | **257** |
| O mundo depende desta unica empresa | 0 | **160** |

Nos posts maduros (40h+) o Buffer bate quase exato: 577/578, 566/569, 530/531.
**Nos novos, mente — e o flag nao protege.** Print do perfil e' mais confiavel
que o Buffer pra post recente, e nao custa cota.

## 3. Fonte de dados melhor, ja' paga e nunca usada

A API do TikTok foi recusada em definitivo (22/08). Mas o **TikTok Studio**
exporta CSV, e o `desempenho.py --importar` ja' existe pra isso desde julho.

Ele traz o que o Buffer **nao tem**: **retencao (% medio assistido)** e
**seguidores ganhos por video**. A retencao e' o gargalo medido do canal (a
audiencia sai aos 0:02) e nunca foi medida por post.

Export: `tiktok.com/tiktokstudio` -> Analytics -> baixar dados (e' na WEB; no
app o botao pode nao existir).

## 4. O que mudou no codigo

- `SLOTS_SP` 6 -> 5 e `MAX_POR_DIA = 5` (`agendar_buffer.py`)
- **dedup offline ligada**: `estado/publicados.json` entra na uniao. A consulta
  para em 4 paginas e e' cega pro que e' antigo — foi essa cegueira que
  republicou um clipe em 25/08. Em 28/08 ela ja' pegou 47 textos que o Buffer
  nao devolvia.
- **`engine/adiados.py`** — clipe vai pro FIM da fila sem ser descartado. Nao e'
  `rejeitados` (que e' "nunca mais"). Tres clipes marcados a pedido do Bryan.
- `.gitignore` limpo: nao nomeia mais nenhum outro projeto.

## 5. Historico do git foi reescrito — leia antes de dar push

Havia **14 commits locais nao enviados**, e **6 eram do canal de comida**. Os
arquivos ja' tinham saido, mas as mensagens carregavam o nome do projeto — e
este repositorio e' **publico**.

Reconstruido: `main` agora tem **7 commits**, todos legitimos deste canal.
Verificado antes de trocar: cherry-picks sem conflito, arvore identica exceto
duas linhas em branco no `.gitignore`, e nenhuma mencao ao outro projeto.

O estado anterior esta' na branch **`backup-antes-limpeza-28-08`**.

**Pode dar push com seguranca.**

## 6. O segundo canal NAO mora aqui

`C:\Users\Administrator\Desktop\Tiktok\Cozinha Importada\` — motor, documentos e
segredos proprios, conta do Buffer propria, repositorios proprios. Ordem do
Bryan: separacao estrita.

**O unico ponto que ainda encosta:** as chaves de API. Gemini, Groq, Cloudflare,
YouTube e NVIDIA sao **fisicamente as mesmas**. O canal de comida ja' ganhou UMA
chave Gemini exclusiva (primeira do rodizio dele), mas as outras 8 sao
compartilhadas. Some quando o Bryan abrir as contas novas.

E o `engine/buffer_cota.py` tem **um contador so'** — se algum dia os dois
motores rodarem do mesmo diretorio, o orcamento se mistura. Hoje nao acontece,
porque as pastas sao separadas.

## 7. Estado ao fechar

- Fila do Buffer: ver `agendar_buffer.py` (nao medido ao escrever este handoff)
- Cota do Buffer do @modofuturo: ~38 requisicoes na janela
- 3 clipes em `adiados`, 4 em `rejeitados`
- Cadencia 5/dia valendo desde 27/08

## 8. Aberto

1. **Rodar `historico.py`** e fechar o teste de cadencia (secao 1).
2. **Exportar o CSV do TikTok Studio** — destrava retencao e a pergunta "a nota
   do Gemini preve views?", aberta desde julho.
3. Lixeira do Drive (~5,40 GB) e `_LIXO_21-08\` (149 MB).
4. Nome de exibicao do canal novo (~04/09).
