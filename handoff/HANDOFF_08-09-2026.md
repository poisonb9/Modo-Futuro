# HANDOFF — 08/09/2026

Continua o `HANDOFF_07-09-2026.md`. Leia também: `PIPELINE.md`, e agora
**`sabedoria/PLAYBOOK_TIKTOK.md` §23**, que é onde o dia inteiro está
destrinchado com número.

---

## 1. ESTADO MEDIDO (08/09, 20:57 UTC)

    @modofuturo          9  (+49h)     @semanestesia.pod    9  (+49h)
    @atefalhar           6  (+41h)     @achadinho.make      5  (+26h)
    @cozinha.internac.   0  VAZIA
    TOTAL 29 posts agendados

    git Modo-Futuro: 84aeeb2 (local == remoto) · suite 64/64 (timeout 90)
    24 commits hoje, TODOS empurrados
    cozinha (bryanaw2121-sketch/pipeline): 4 pronto · 1 disparado · 1 desistido

⚠️ **O @truque.importado agora é `@achadinho.make` no TikTok.** O Bryan
renomeou. O `nome_buffer` continua `truque.importado` de propósito — é com
ele que a guarda `CANAL_ESPERADO` compara e por ele que os workflows escolhem
o token. O export do TikTok chega como `Content_achadinho.make.zip`.

---

## 2. ⚠️ O QUE DEIXOU DE SER VERDADE HOJE

O Bryan exportou o TikTok Studio das 5 contas: 60 dias de série diária e os
15 melhores posts de cada canal, com **view real**. Foi a primeira vez que deu
para conferir as crenças do projeto contra a nossa própria operação. **Quatro
caíram.**

**2.1 O "colapso de agosto" não foi punição. Foi a operação parando.**

    03/08  2 posts     81 views
    04/08  0 posts      3
     ...   0 posts   1 a 6      <- QUINZE DIAS SEGUIDOS SEM POSTAR
    22/08  5 posts   3795       <- o dia em que voltamos a postar

**2.2 O "colapso de 25/08" nunca existiu.** 1843 views naquele dia, entre 780
e 2189 dos vizinhos.

**2.3 A tese "duplicata derruba o alcance" perdeu os dois casos.** Um era a
pausa, o outro não aconteceu. Evitar duplicata continua certo (republicar sem
alteração é `[CONSENSO 4]` do corpus), mas **não há mais nenhuma medição
própria a favor dela** — parem de citá-la como fato medido.

**2.4 O rótulo de IA não explica nada.** `isAiGenerated` entrou no código em
25/08; a recuperação começou em 22/08. Causa não vem depois do efeito.

**2.5 Não há punição de plataforma.** `Verificação de conta` nas CINCO contas:
"Nenhum problema pendente". A verificação cobre item por item, inclusive
**"Possibilidade de a conta ser descoberta"** — o que estaria marcado num
shadowban. Ressalva do próprio TikTok: não cobre status de Monetização.

**2.6 E a minha própria hipótese caiu.** Achei "texto de outra plataforma
queimado" nos dois piores clipes e apresentei como o achado que fecharia tudo.
Classifiquei 13 frames à mão: com texto 230 views de média, sem texto 452 —
mas **n=4 no grupo com texto, e o clipe de 527 views do @semanestesia abre com
"spoken by JOCKO WILLINK" em letras gigantes**. Não sustenta. A regra fica no
motor (custa zero e alinha com a política oficial do TikTok), mas parou de ser
explicação.

**O que sobra:** 54.199 views viraram 78 seguidores. **0,144%.** Não há nada
escondido tirando vocês do ar — o conteúdo alcança e não converte.

---

## 3. AS QUATRO CALIBRAGENS DO MOTOR (todas medidas, todas atribuíveis)

Estão em `estado/calibragens.jsonl`, **cada uma com a condição que a derruba**.

| # | mudança | medição | onde |
|---|---|---|---|
| 1 | título MOSTRA a coisa, não promete explicação | 1,48x contra 1,07x (**+37%**), curtida 3,53% contra 2,62% | `engine/selecao.py` |
| 2 | abertura prefere AÇÃO na tela, rosto com olhos, foge de texto alheio | frames do melhor e do pior de 2 canais | `engine/selecao.py` |
| 3 | promessa não vira AMEAÇA (palavra de risco) | 0,94x contra 1,36x (**−31%**), curtida −30%, nos 4 canais | `engine/selecao.py` |
| 4 | slot 11:33 → 13:10 | 11:33 era 0,97x, o único abaixo da mediana; 16:27 é 1,50x | `agendar_buffer.py` |

⚠️ **Número no título rendeu +27%.** ⚠️ **Nome de pessoa no título rendeu
−18%** — hipótese minha que a medição derrubou.

⚠️ **A nota do motor continua sem prever nada: correlação 0,00 com views
(n=53).** Ela mede se o corte está bem construído, não se alguém quer ver.

### Como o Gemini trabalha diferente agora

O prompt de seleção passou de 30.128 para 35.864 chars. Em concreto:

- **campo novo `abertura_mostra`** (`acao | pessoa_falando | texto_alheio |
  parado`): ele passa a REGISTRAR o que se vê nos 2 primeiros segundos;
- **regra nova de escolha do trecho**: preferir o instante em que a coisa
  ACONTECE ao instante em que alguém fala sobre ela; rosto com olhos quando só
  houver pessoa; desviar de frame com legenda de outra plataforma;
- **o campo `titulo` deixou de dizer só "chamativo"** e passou a mandar mostrar
  a coisa, a cena ou o resultado, com número quando houver na fala;
- **e a não enquadrar a promessa como ameaça.**

⚠️ Os dois primeiros são DESEMPATE, não critério único — trecho com imagem boa
e fala fraca continua ruim, e há teste negativo guardando esse limite.

---

## 4. ⚠️ AS DUAS MUDANÇAS QUE EU NÃO FIZ, DE PROPÓSITO

As quatro de cima são **atributo de post** — cada post tem ou não tem. Por
isso são medíveis no próximo export: compara-se post com post, no mesmo mês.

Estas duas **não são**, e valeriam para todo clipe novo ao mesmo tempo:

1. punch-in cíclico de 6,5s → ~3s nos primeiros 12s (`render.py:_PUNCH_PERIODO_S`)
2. a tarja do título, que ocupa 25-30% do topo (`engine/legendas.py`, 0.30)

Sem grupo de comparação dentro do mês, o efeito ficaria impossível de separar
das quatro medidas — e as duas vêm de `[CONSENSO 2]` do corpus, não de medição
nossa. **Ficam na fila, uma por vez, depois do próximo export.** Ver §23.7 do
playbook.

---

## 5. CONSERTOS DO DIA (o padrão que apareceu QUATRO vezes)

**O trabalho era feito e o resultado descartado antes de chegar em quem
precisava dele.**

| o que | quanto tempo | causa |
|---|---|---|
| cozinha em ZERO | 6 dias | `subir_cortes_drive` falhava com 404 e derrubava o job ANTES de "Publicar na Release" e "Enfileirar no Buffer" — sem `continue-on-error` |
| medição de views | 5 dias | sem `pip install`, `historico.py` morria, `publicados.json` não nascia, e `git add` de caminho inexistente matava o commit |
| medição de views, de novo | mesmas horas | consertado o de cima, caiu em `estado/serie_views.jsonl` estar no `.gitignore` — `git add` de caminho ignorado é RECUSADO |
| rede antiduplicata | desde sempre | `publicados.json` era gitignored: existia AQUI e nunca na NUVEM, que é quem decide |

Outros:
- **guarda de TEMA** (`engine/tema.py`): bruto na pasta errada não vira corte.
  Nasceu dos 8 clipes de IA publicados no @semanestesia;
- **guarda de TRECHO** (`engine/trechos.py`): mesma fonte + mesmo segundo, mesmo
  com título diferente. 3 duplicatas interceptadas antes de ir ao ar;
- **cadência de download** (`engine/cadencia.py`): um por vez na máquina, com
  intervalo entre TODOS os canais. Falha FECHADA;
- **freio dos cortes** (`engine/freio.py`): `PAUSA_CORTES` para os três
  disparadores. Está SOLTO;
- **ciclo semanal** (`ciclo_semanal.py` + tarefa `ModoFuturo_Ciclo_Semanal`,
  diária 13:00 local): repõe FONTE quando um canal seca.

---

## 6. A ROTINA MENSAL (nova, e não pode falhar)

O histórico do TikTok **para em 60 dias**. O que não for exportado dentro da
janela some para sempre.

    studio.tiktok.com -> Analytics -> periodo -> exportar, nas 5 contas
    python importar_overview_tiktok.py --pasta "<pasta>"        (serie diaria)
    python importar_metricas_tiktok.py --arquivo Content_X.zip --canal X

⚠️ O export do TikTok **vem quebrado**: numa das linhas as colunas deslocam e
"Total views" vem com a URL dentro. A leitura se ancora no LINK, não na coluna.

---

## 7. PENDENTE

**[BRYAN]**
1. **Seguidores por vídeo** — abrir um post no Studio e ver se aparece "novos
   seguidores". Mede DIRETAMENTE a única coisa que importa (0,144%), em vez de
   usar view e curtida como aproximação. **É o alvo de maior valor que existe.**
2. **Retenção por segundo** — a curva de cada vídeo. Toda a §4.4 do playbook
   depende dela, e ela destrava a decisão sobre a tarja do título.
3. **Criar conta na Braip** — trava o `PRODUTO_DIGITAL.md` (vem de 07/09).
4. **Ligar a "Verificação automática de direitos autorais da música"** no
   Studio — está desligada, e o áudio original vai por baixo da dublagem.

**[EU]**
5. Primeira frase falada dos 2 segundos: só existe no `FALA.txt` do artefato do
   runner, não chega aqui. É mudança de workflow.
6. Reconstruir procedência da fonte (clipes antigos não têm `fonte_id`).
7. `repor_fila` da cozinha: existe e roda 12:00 UTC, mas o canal ainda não
   voltou a postar.

⚠️ **Eu não rodo entre turnos.**

---

## 8. O QUE NÃO FAZER

Continua tudo do handoff de 07/09, mais:

- **não citar "duplicata derruba o alcance" como fato medido** (§2.3);
- **não citar o rótulo de IA como causa da recuperação de agosto** (§2.4);
- **não ligar as duas mudanças globais da §4 juntas**, nem antes do próximo
  export;
- **não confiar na view do Buffer** para ranquear nada: ela marcou ZERO em
  posts de 6 e 7 dias que tinham 338, 359 e 142 views reais. Curtida serve;
- **não mexer no slot das 16:27** — é o melhor medido, e mudá-lo junto com
  outra coisa impede atribuir o efeito.

---

## 9. BACKUP DO SISTEMA (feito em 08/09, 21:08 UTC)

**Local:** `Desktop\Tiktok\BACKUP_SISTEMA\`

    LEIA_PRIMEIRO.md          o sistema inteiro explicado pra quem chega do zero
    repositorio/              Modo-Futuro.bundle — historico COMPLETO do git,
                              conferido com `git bundle verify`
    tarefas_agendadas/        XML das 2 tarefas do Agendador (vigia e ciclo)
    scripts_locais/           os .ps1 que o Agendador chama
    estado/                   25 arquivos de estado, incluindo a rede
                              antiduplicata e a serie de views
    SEGREDOS_NAO_SUBIR/       .env, CREDENCIAIS.md, tokens do Drive/GitHub/
                              TikTok/Buffer
    + HANDOFF, PLAYBOOK e PIPELINE

**Nuvem:** Drive da conta `labzirkonart` (a mais folgada, 11,33 GB livres),
pasta **"BACKUP Modo Futuro"**, arquivo
`BACKUP_SISTEMA_nuvem_08-09-2026.zip` (2,5 MB, 35 arquivos).

⚠️ **O zip da nuvem NAO tem a pasta de segredos.** A exclusao e' por PREFIXO
DE CAMINHO, nao por nome de arquivo — filtrar por nome deixaria passar
qualquer arquivo novo que alguem pusesse la' dentro depois. Conferido: zero
vazamentos no zip.

⚠️ **Consequencia:** o backup da nuvem sozinho NAO reconstroi a operacao. Ele
tem o codigo, o historico, o estado e a documentacao — mas as chaves so'
existem nesta maquina. Se ela morrer, os canais param ate' o dono repor as
credenciais. **Subir os segredos criptografados (7-Zip AES-256, senha por
fora) e' decisao dele, e esta' na §7 do pendente.**
