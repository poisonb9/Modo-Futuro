# HANDOFF — 04/09/2026, sessao de CONSERTO

Leia antes: `PIPELINE.md` (mapa), `FASE2.md` (o que vem agora) e o
`handoff/HANDOFF_03-09-2026_parte2.md` (a sessao anterior, de medicao).

⚠️ **O adendo 2 daquele handoff diz "a fabrica esta' seca". Estava errado**, e
o erro tem nome: o `repor_fila` so' conta o manifesto quando o canal esta'
ABAIXO do piso. Com a fila exatamente NO piso ele nao conta, e o "0 ainda nao
agendado" era o valor de quem nao olhou. Medido depois: 27 clipes prontos.

---

## 1. ESTADO MEDIDO (04/09 ~23:40 UTC)

    @modofuturo             10  (+61h)     @atefalhar          10  (+61h)
    @semanestesia.pod        8  (+45h)     @truque.importado    5  (+24h)
    @cozinha.internacional   0  VAZIA
    TOTAL 33 posts agendados
    fonte: `python painel_filas.py`

    fila de cortes, NUVEM: 16 pronto · 15 desistido · 2 pendente · 1 disparado
                           · 1 sem_fonte   (35 itens)
    dos 15 `desistido`, 12 sao os pausados por `groq_25mb_recorte_1200s`

    estoque REAL por canal (`medir_estoque.yml`, roda na nuvem, nao agenda):
      truque.importado  1 pronto  · semanestesia.pod  8 prontos
      cozinha           0         · modofuturo e atefalhar: fila cheia,
                                    o agendador para antes de contar

⚠️ **O teto NAO serve como estoque.** Pelo manifesto offline o truque dava
"ate' 19"; o real era **1**. As guardas do Buffer cortaram 61% no modofuturo
(69 -> 27). Nunca extrapole o teto.

---

## 2. O QUE FOI CONSERTADO (5 commits, suite 53/53 com `timeout 90`)

    e222988  FASE2.md
    2860b02  canal nunca se adivinha: registro unico, recusa em vez de palpite
    868c0a0  janela != recorte (o defeito do Groq)
    3520dea  sonda de cota ANTES do download
    51e14c4  uma tabela de canais so': as 5 copias derivam do registro

### 2.1 Clipe sem canal NAO e' mais "modofuturo"
`agendar_buffer.py` chutava o canal de tecnologia quando o campo vinha vazio.
Hoje os 161 clipes tem canal, entao nada mudou AGORA — muda quando entrar
clipe de pipeline nova sem o campo, que e' o que a fase 2 traz.

### 2.2 A janela voltou a ser escopo de busca
A fila mandava a janela de 1200s pelo `--recorte`, que significa "o trecho JA'
E' o clipe". O FLAC dava 36-39 MB contra o teto de 25 MB do Groq: 12 runs,
~11h de runner, zero clipe. Agora o Gemini escolhe DENTRO da janela e os
tempos voltam pra linha do bruto. **Nao muda o que se publica** — volta ao
normal dos outros canais.

### 2.3 Sem cota, o run morre em segundos
A selecao nao tem reserva (a traducao tem). O `guarda_cota.py` roda logo apos
o pip e antes de Chatterbox, amostra de voz e download. A mensagem carrega
`todas as chaves esgotadas` DE PROPOSITO: e' marca que o `falhou_por_cota()`
reconhece, senao o aborto viraria "defeito da fonte" e tres deles expulsariam
um video bom da fila.

### 2.4 Um batismo de canal so'
`engine/canais_registro.py` e' a unica fonte. `nome_buffer` (o que a guarda
CANAL_ESPERADO compara) e `arroba` (o @ do TikTok) sao campos SEPARADOS — a
cozinha e' `cozinha.importada` no Buffer e `@cozinha.internacional` no TikTok,
e usar os dois como sinonimo fazia o token cair no `else` e sair o do
modofuturo.

---

## 3. ⚠️ TRES VEZES UM TESTE VERDE PROTEGEU O DEFEITO

Este e' o achado que mais vale pra proxima sessao:

  - `teste_vigia_manda_canal` EXIGIA `cozinha.internacional` — o nome que
    fazia o token cair no `else`;
  - `teste_fila_passa_recorte` tinha na docstring, escrito, que "recorte e' a
    janela" — o mal-entendido inteiro, fixado em teste verde;
  - a fusao das tabelas quebrou o painel (`? sem token` na cozinha) e **os 53
    testes passaram**. Peguei rodando o `painel_filas.py` de verdade.

**O padrao:** teste que fixa a GRAFIA de um acordo entre dois modulos congela
o mal-entendido em vez de pegar. E suite verde nao substitui rodar o comando.

---

## 4. O QUE FOI FEITO NA MAO, FORA DO CODIGO

  - **8 clipes do Goggins reetiquetados** `modofuturo` -> `semanestesia.pod`
    no manifesto da release `clipes-09-2026` (eram 5 pelo titulo; a busca pela
    FONTE achou 8). modofuturo 98->90, semanestesia 20->28.
  - **4 posts removidos da fila do @modofuturo** (DeletePostSuccess), porque
    eram Goggins agendados no canal de chips. Repostos por 4 clipes de chips.
  - ⚠️ **NAO usei o `rejeitados.json`**: ele bloqueia por texto e e' GLOBAL,
    entao marcaria os clipes tambem no @semanestesia, que e' onde eles devem
    sair. A correcao do rotulo resolve sozinha.

---

## 5. PENDENTE DO BRYAN (o que o loop tem de retomar)

  1. **@truque.importado seca primeiro**: 5 posts, +24h, e **1** clipe pronto.
     Vídeo novo na pasta `Truque Importado` do RAW. Nao e' codigo.
     ⚠️ `MODO FUTURO` tambem esta' sem fonte nova, mas tem 13 prontos.
  2. **Cozinha em 0.** O patch esta' em
     `C:\Users\Administrator\Desktop\Tiktok\conserto_fahrenheit.patch`
     (2 commits, suite deles 18/18). O fork pela API foi RECUSADO: o PAT do
     `.env` da 403 "Resource not accessible by personal access token". Sem
     fork nao ha' PR. Caminhos: `git am` no clone do `pipeline`, ou dar Write
     ao `poisonb9` (⚠️ o convite vai pro e-mail da conta poisonb9).
     ⚠️ O Bryan perguntou se ha' outra opcao AUTOMATICA. Nao medi ainda: ficou
     no meio da busca por um clone local do `pipeline` (ele interrompeu).
  3. **Reacender as 15 janelas do @semanestesia.** Decidido: devolver DOIS
     itens de `desistido` pra `pendente`, olhar o run, e so' entao os outros
     dez. Os 12 sao os que tem `pausado_por == "groq_25mb_recorte_1200s"`.
     ⚠️ O caminho `--janela` NUNCA rodou em producao. Por isso dois primeiro.

---

## 6. ARMADILHAS DESTA SESSAO (custam minuto, todas medidas)

  - `load_dotenv()` sem caminho QUEBRA quando o script vem por `stdin`
    (`assert frame.f_back is not None`). Use `load_dotenv(".env")`.
  - Script no scratchpad precisa de `PYTHONPATH="$PWD"` pra importar o projeto.
  - `vigia_raw.videos_em_raw()` sozinho devolve VAZIO: o RAW vive na conta
    `reserva`. Use `videos_todas_contas()`.
  - Procurar nome solto em YAML acha COMENTARIO: `WF.index("chatterbox-tts")`
    casou com um comentario 60 linhas antes do passo. Procure o comando.
  - O `.env` local NAO tem `BUFFER_TOKEN_*`; so' o `buffer_token.txt`, que e'
    do modofuturo. Medir os outros canais exige a nuvem.
  - Guardar `if "x" not in s` depois de escrever um comentario que contem "x"
    da' falso positivo — foi assim que um import nao entrou no `repor_fila`.

---

## 7. O QUE CONTINUA RODANDO SOZINHO

    cron cortar_fila   */30, best-effort (1,8h a 4,8h medidos)
    encadeamento       cada corte que termina dispara o cortar_fila
    vigia do RAW       10 em 10 min, 1 por passada
    repor_fila         12:00 UTC (⚠️ so' age ABAIXO do piso de 5)
    desempenho.yml     de hora em hora

⚠️ Esta sessao nao deixou monitor nenhum. **E eu nao rodo entre turnos.**
