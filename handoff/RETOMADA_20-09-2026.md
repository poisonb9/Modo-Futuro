# RETOMADA — 20/09/2026 (escrito 20/09 ~01:30 BRT, antes do /clear)

Sessão de 19/09 ~20:00 → 20/09 ~01:30 BRT. **13 commits de site** (`a5d480b` → `34215a0`).
O anterior é [`RETOMADA_19-09-2026.md`](RETOMADA_19-09-2026.md) — ler o rodapé dele para
a arquitetura do vidro (não reaprender) e o congelamento.

---

## ESTADO NO FECHAMENTO

```
local = remoto      34215a0 (main), Desktop\Tiktok\Modo-Futuro e ATUALIZADA\clip_engine iguais
                    (clip_engine tem ferramentas/ssr/node_modules instalado — jsdom)
NO AR               achadinhototal.com.br, 24 endereços conferidos (23 + links.json)
                    HTML 310 KB brutos / 71 KB no fio (era 582 / 143)
⛔ CONGELADO        transparência da barra de abas (#abas) e do console (#console): .vidro,
                    --vidro-*, shader, .espelho-vivo — ordem do Bryan 19/09 17:14.
                    Único toque hoje, POR ORDEM DELE: sombra externa da barra removida.
Suíte              teste_nao_publicar 5 verdes · teste_indexacao verde · teste_ssr_primeira_tela
                    verde (novo, 6 blocos c/ negativo) · teste_categoria_externa, topo_5_mais_5 verdes
                    JÁ FALHAVAM ANTES (conferido com stash): teste_topo_coerente (1),
                    teste_economia (3), teste_vitrine_produtos_reais (KeyError '_todos')
```

## O QUE SUBIU HOJE (na ordem; tudo julgado por print/vídeo do iPhone dele)

| Commit | O quê | Por quê |
|---|---|---|
| a5d480b | barra de abas sem sombra externa (`--vidro-sombra`) | "sombra ghost" sobre os cartões |
| c14dd0f | placa da vitrine: texto escuro FIXO (#16151c) | no modo escuro `--tinta` é branco → título sumia sobre foto branca |
| 11c3a76 | troca de aba por **View Transition** (Safari 18+, .26s; `#abas` fora do fundido); preço da vitrine em Poppins 700 tabular, `R$` menor, alinhado pela base | vídeo: redesenhar+rolar era salto seco; fonte mono "não premium" |
| e2b0af9 | vão console→1º cartão 22→14 px | pedido dele |
| a99222c | convite do Telegram escondido enquanto `#grade:empty` | HTML cru no recarregar |
| 66670b0 | **links de afiliado fora do HTML** → `links.json` (`LINKS_ARQUIVO`, `carregarLinks`, `esperarLinks`); índice estático aponta `?p=<id>`; HTML embute link só de topo+vitrine+2 | MEDIDO: link do Ali tem 1.065 chars aleatórios ×2 por produto = ~310 KB dos 582 |
| ae5a5cb | `.js #indice-estatico{display:none}` (classe `js` na 1ª linha antes do CSS); `#grade:empty{min-height:70vh}` | Safari pintava a lista azul do crawler |
| a66ccae | preço dos **cartões** no mesmo desenho da vitrine (Poppins, `.rs`, tabular; `.de` em Poppins) | "padronizar nos outros anúncios" |
| b004d3c | esqueleto: 2 cartões pulsando em `#grade:empty::before/::after` | vazio durante o carregar |
| 679f90b | (superado) cópia em localStorage | — |
| 34215a0 | **SSR da primeira tela** (ver abaixo); sai o localStorage | "vamos de ssr pq ainda mostra" |

## SSR DA PRIMEIRA TELA — como funciona (não reaprender)

- `ferramentas/ssr/ssr.js` (node + **jsdom**, `npm install` na pasta; `node_modules/` ignorado).
  Lê o HTML montado no stdin, roda **os scripts da própria página** (`runScripts: outside-only`
  + `win.eval` na ordem), espera 1 s, e cola no HTML **texto** (não no DOM do jsdom): celas da
  prova, filhos de `#chips`, de `#vitrine`, e os 8 primeiros `#grade > .item`.
- Polyfills: `matchMedia` (reduced-motion = **SIM**, senão captura o meio da contagem de 700 ms —
  aconteceu: 1.324 no lugar de 1.666), `IntersectionObserver`, `fetch` rejeita, `getContext` null.
- Cópia **inerte**: `<a>` internos viram `<span>` (parser HTML não aceita `a` dentro de `a` — partia o
  cartão em dois, medido), `<canvas>` fora.
- `publicar_bio.ssr_primeira_tela()` roda **antes** de `_carimbar`; sem node/jsdom ou falha →
  publica sem SSR e imprime aviso (preço na hora > primeira tela).
- Na página: `montarProva` limpa `.cela/.fim` e marca `PROVA_PRE_MONTADA` → `contarNumeros` não conta
  de 0 (seria um reset). ⚠️ Isso desliga a contagem "1x" que ele quis em 18/09 quando há SSR —
  **decidir com ele se quer de volta** (só faz sentido contando a partir do valor já pintado).
- Script pequeno antes do grande: `?p=` ou `#hash` na URL limpam vitrine+grade pré-montadas
  (ordem diferente da abertura); fica o esqueleto até o script chegar.
- Cartões sem link embutido não têm `href` até `links.json`; clique antes espera (`esperarLinks`).
- A tarefa S4U `AchadinhoTotal_Publicar_Ao_Mudar` publica do clip_engine → jsdom **já instalado lá**.
  Se o clone mudar de lugar: `cd ferramentas/ssr && npm install`.

## ⛔ ERROS MEUS DE HOJE (para não repetir)

- **Bytes com não-ASCII** em `b'''…'''` (travessão, ⭐) estouraram 4 scripts de edição; e um deles
  deixou um commit entrar **só com metade** da mudança e mensagem errada (`a5d480b`). Editar por
  `str` com `newline=''`, ou anchors ASCII.
- **`sed | od` normaliza CRLF**: achei que o arquivo era LF; é 100% CRLF. Medir com Python (`count(b'\r\n')`).
- **`$TMP` dentro de `python -c "…"`** vira `\U` escape. Usar `os.environ['TMP']`.
- Afirmei "script morto" olhando um print que ainda tinha a **linha azul de progresso** do Safari.
  Ler a barra antes de diagnosticar.
- Capturei a prova **no meio da animação** no SSR (1.324 ≠ 1.666). Todo harness que copia DOM tem
  de esperar/pular animações. O teste agora compara com `produtos_todos + externas`.
- Cachê em localStorage: **duplicaria as celas** (montarProva anexa) — só vi porque testei o
  pré-script num iframe com o HTML cortado antes do script grande (técnica boa: repetir).

## 🔴 O QUE O BRYAN FAZ (inalterado + hoje)

1. **Recarregar o site no iPhone** e dizer se ainda vê esqueleto (com SSR não deveria; se vir, mandar
   print COM a barra do Safari visível).
2. Veredito do **toque na aba** (pílula com mola + View Transition).
3. Buffer → reconectar 4 canais (views cegas desde 08/09) · Redirect Rule www→raiz · Reclame Aqui
   das lojas · termômetro R$ 16,90 no celular · BotFather · foto quem-somos (depois).
4. Decididos e mantidos: Venancio e Ali-via-Awin **fora**; Clovis **fora** do site.

## 🟠 O QUE EU FAÇO NA PRÓXIMA (ordem)

1. Se ele quiser: preço do **herói** (`.vivo-preco`, ainda mono) na mesma Poppins (1 linha).
2. Se ele quiser a contagem de volta: contar **a partir** do valor pré-montado, não de 0.
3. Os 3 testes que já falhavam (topo_coerente, economia, vitrine_produtos_reais `_todos`) — medir
   se é dado ou código antes de mexer.
4. Congelar features até a 1ª venda (decisão 5 de 18/09) — hoje foi tudo correção de defeito
   visto por ele, exceto o SSR (decisão explícita dele).

## Ideias abertas (só com ordem)
`animation-timeline: scroll()` para a tira; laje 3D (`?v3=1`); chips estáticos no HTML já não são
necessários (SSR cobre); Gemini imagem precisa de faturamento.

---
## ⭐ RODAPÉ PARA O /clear — 20/09/2026 ~01:30

**Onde estamos.** Site em Liquid Glass aprovado; barra e console **congelados** (19/09 17:14, tag
`checkpoint-vidro-aprovado-19-09`). Hoje: 13 commits de correção julgados por print do iPhone —
sombra da barra fora, placa/preço da vitrine e dos cartões em Poppins, fundido na troca de aba,
página 582→310 KB (links de afiliado em `links.json`), e **SSR da primeira tela** (`34215a0`):
o publicador roda o script da página em jsdom e cola prova+chips+vitrine+8 cartões no HTML.
Último no ar: `34215a0`, 24 endereços conferidos, 71 KB no fio.

**Primeira coisa:** perguntar se o recarregar no iPhone ainda mostra esqueleto (não deveria) e o
veredito do toque na aba.

**Como eu trabalho aqui:** edito `Desktop\Tiktok\Modo-Futuro\paginas\todos.html` (100% CRLF —
scripts Python em `str` com `newline=''` ou bytes ASCII; anchors sem ⭐/travessão); `node --check`
nos scripts extraídos; `teste/teste_nao_publicar.py` + `PYTHONPATH=. teste/teste_ssr_primeira_tela.py`;
commit + `git pull --rebase` + push; publico do clone `YouTube videos para Google Drive\ATUALIZADA\
clip_engine` (`rm -f .git/index.lock; git checkout -q paginas/todos.html; stash; pull; pop;
python -X utf8 paginas/publicar_bio.py --subir`). Dublê local: `.claude/launch.json` → `site-duble`
(porta 8766, scratchpad/site); gero com `montar_catalogo()` → `ssr_primeira_tela()` → `_carimbar()`.
Para ver o pré-script: iframe com o HTML cortado antes do `<script>` grande.

**Pendências dele:** Buffer 4 canais; Redirect Rule; Reclame Aqui; termômetro R$16,90; BotFather;
foto quem-somos; Venancio/Ali-via-Awin e Clovis fora (decididos).


---

# SESSAO DE 20/09/2026 (manha, ~04:00 -> 11:30 BRT) — continuacao

## DECISAO DO BRYAN QUE MANDA NO DESENHO DAQUI PRA FRENTE

> "Essa parte que passa por trás não quero que mexa, inclusive eu gostei demais
> de como as cores se comportam. Agora sobre a parte de cima sim."
> — 20/09, com print e video (08:20) do campo de busca e dos chips.

**A regiao do `#console` onde o conteudo passa POR TRAS do vidro — a busca e os
chips — esta APROVADA E FECHADA.** As cores dos produtos atravessando o material
sao o efeito que ele quer preservar. Nao mexer em: `--vidro-*`, `.vidro`,
`backdrop-filter` do container, `.busca-caixa`, `.chip-ordem`, `#abas`.

O que ele abriu foi **so' o mostrador** (`.prova`): o bloco `1.666 VIGIADOS /
99 / R$ 2.981 / CONFERIDO HA...`.

⛔ **Eu errei o escopo uma vez e publiquei assim.** A primeira versao pos
`brightness(.965)` no `#console` inteiro — que inclui a regiao que ele ama.
Corrigido no mesmo dia: container voltou ao original, tudo foi pro `.prova`.

## POR QUE NENHUM TRUQUE DE FUNDO RESOLVE O MOSTRADOR (medido, nao reaprender)

```
#console        position: sticky, top: -106px   -> o mostrador SOME ao rolar
fundo do <html> rgb(255,255,255) chapado no claro
.luz            display:none  (decisao dele, 4a1bae3 "fundo branco liso")
```

O mostrador so' e' visto **parado no topo**, e la' nao ha' nada atras.
`blur()` e `saturate()` sao convolucoes: sobre campo uniforme devolvem o proprio
campo. `brightness()` e' o unico filtro que ainda faz efeito (desloca o valor),
e sobre branco so' da' pra escurecer. **Ele recusou por luz atras** ("nao quero
a luz dourada"). Logo: a solucao tem de ser SUPERFICIE.

## A SOLUCAO (dos acervos, so' DEMONSTRADO)

- **Grao de vidro fosco** — tres fontes independentes do acervo liquid-glass
  mandam textura/ruido sobre o glassmorphism. SVG `feTurbulence` usado como
  **imagem** (`background-image`), nunca como `filter: url()` sobre backdrop —
  esse e' o que o Safari nao renderiza.
- **Aro conico** — `conic-gradient` em pseudo-elemento (css-frontend, Kevin
  Powell) no lugar do linear de 3 paradas: o reflexo ANDA pelo perimetro.
  ⚠️ `::before` de `display:grid` vira item do grid; `position:absolute` tira.
- ⚠️ **Nada de `backdrop-filter` no `.prova`**: vira containing block de
  `position:fixed` (medido em 19/09, as folhas de menu abriam dentro do console).

## O QUE SUBIU HOJE

| Commit | O que |
|---|---|
| `c1a65ca` | arranque fora do parse (ganho parcial medido: -80 ms) |
| `deaea58` | **motor fora do HTML** (`motor.js?v=<sha>` + `defer` + cache 1 ano) e **DOCTYPE + lang** (o site rodava em quirks mode) |
| `5fb5828` | **SSR cobre o topo**: brasao e linha viva deixam de ser buraco na 1a pintura |
| `64cf039` | **backup do que esta no ar** versionado (`site_no_ar/`, 29 arquivos, 3,49 MB, + config do Cloudflare) |
| `565e21c` | vidro do console no claro — **escopo errado, corrigido depois** |

Peso no fio: HTML 71 KB -> 25 KB; com o brasao pre-montado, 36 KB.
Recarregar caiu ~50% e o motor vem do cache.

## 🔴 PARADO ESPERANDO O BRYAN

1. **aba ativa "Inicio"** — contraste **1,02:1** no claro, 1,11:1 no escuro
   (branco sobre pilula clara). Esta dentro do `#abas`, que segue **congelado**.
2. **chip ligado "Tudo"** — **1,11:1 so' no escuro** (no claro vai a 18:1).
3. **cabecalhos de seguranca** — ja' medido: o Pages ja' manda
   `x-content-type-options` e `referrer-policy`; faltam CSP, `frame-ancestors`,
   `Permissions-Policy` e HSTS. Os 18 dominios que o site carrega ja' estao
   levantados (o `img-src` e' lista aberta: muda a cada anunciante novo).
4. **RLS no Supabase** — `SELECT` anon em `clique_produto` e `busca` responde
   **200 com lista vazia**. Isso NAO distingue "RLS barrando" de "tabela vazia".
   So' o painel do Supabase resolve. Nao escrevi na tabela de producao pra testar.

## Backup

`python -X utf8 ferramentas/backup_do_ar.py` regrava `site_no_ar/`.
NAO tem DNS nem SSL da zona (403: o token de publicacao nao le').
