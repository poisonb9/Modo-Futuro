# HANDOFF — 09/09/2026

Continua o `HANDOFF_08-09-2026.md`. Leia também `PIPELINE.md` (mapa, e a §8
ganhou uma REGRA ABSOLUTA nova) e `sabedoria/PLAYBOOK_TIKTOK.md` §23.9.

---

## 1. ESTADO MEDIDO (09/09, no fechamento)

    git: local == remoto  ·  suite 69/69 verde (timeout 150)
    solto na raiz: relato_cortes.txt (untracked, como estava)
    nada em voo na nuvem · nada disparado sem pedido

⚠️ **O push voltou a funcionar.** O 403 que travava desde 08/09 não existe
mais com o token de `github_token.txt`. O comando que funciona:

    TOKEN=$(cat github_token.txt); AUTH=$(printf "x-access-token:$TOKEN" | base64 -w0)
    git -c http.extraheader="Authorization: Basic $AUTH" push origin main

⚠️ Se der `rejected`, a nuvem commitou por cima (o `desempenho.yml` grava
estado). `git pull --rebase origin main` resolve — os commits dela tocam só
`desempenho.jsonl`, `estado/publicados.json` e `estado/serie_views.jsonl`.

---

## 2. O QUE FOI MEDIDO HOJE, E MUDA A ESTRATÉGIA

### 2.1 Seguidor por vídeo — 18 posts do @modofuturo, lidos um a um

`estado/seguidores_por_video.jsonl` · playbook §23.9

    12.006 views -> 37 seguidores (0,308%). UM post carrega 28 deles.

    correlacao com SEGUIDOR      n=18     sem o outlier
      curtida %                  +0,35        +0,52   <- o unico que sobra
      completo %                 +0,22        +0,17
      tempo medio                -0,06        +0,18
      views                      +0,95        +0,18   <- era so' o outlier

**RETENÇÃO NÃO PREVÊ SEGUIDOR.** Os três posts de 22/08 (mesmo canal, mesmo
dia — cadência e algoritmo controlados) dão a ordem INVERSA:

    A maquina de 400 milhoes    728 v  20,85s  7,8%  ->  1 seg
    Como 1 poeira / 1 milhao   1009 v  16,11s  9,1%  ->  1 seg
    As regras extremas         2473 v  10,63s  6,9%  -> 28 seg

**NÃO HÁ PATAMAR DE ALCANCE:** 1009 views deu 1 seguidor; 309 deu 1.

⚠️ **E eu errei no meio.** Com n=11 declarei a curtida falsificada por causa
de UM contraexemplo e corrigi o playbook. Com n=18 ela é o sinal mais forte.
A correção foi desfeita; o erro ficou escrito na §23.9.

### 2.2 Título que PERGUNTA não converte — a calibragem 1, confirmada

    titulo PERGUNTA ("Por Que...", "?")   n=4    2153 views    0 seg   0/4
    titulo AFIRMA (mostra a coisa)        n=14   9853 views   37 seg   7/14

Os 4 títulos-pergunta foram publicados **entre 28/08 e 05/09, todos ANTES**
da calibragem de 08/09 que já proíbe o formato. **O motor já parou de
produzi-los.** Registrado em `estado/calibragens.jsonl` com o `cai_se`.
Ressalva: n=4, ~14% de chance por acaso. Vale por CONCORDAR com a medição
anterior (n=75, em view e curtida).

### 2.3 Conversão POR CANAL — `estado/conversao_por_canal.jsonl`

    semanestesia.pod   Sem Anestesia      19 seg   276 curt   6,9%
    atefalhar          Ate Falhar         10 seg   176 curt   5,7%
    cozinha.importada  Achadinho Chef      5 seg   116 curt   4,3%
    truque.importado   Achadinho Make      8 seg   460 curt   1,7%  <- fora

O @achadinho.make tem MAIS curtida que os três somados e converte 4x pior.
**A taxa de conversão é propriedade do CANAL, não só do post** — nenhum
ajuste de corte fecha 4x. Hipótese (n=4 canais, NÃO é achado): é o único
cuja grade é dominada por UMA pessoa (5 dos 6 primeiros).

### 2.4 O Studio foi ESGOTADO

As três abas de cada post foram lidas até o fim. A aba *Espectadores* não
traz `Novos × Retornaram` nos posts de ~550 views, que eram os que
separariam "público novo" de "muito alcance". **Reanalisar os 18 não produz
nada — o limite é de DADOS, não de análise.** Ver §23.9.

---

## 3. O DOWNLOAD DO YOUTUBE — resolvido no diagnóstico, não na nuvem

Quatro obstáculos, descobertos um por vez com runs #10 a #13:

    #10  bgutil plugin 2.0.0 x script 1.3.1   -> versao virou UMA variavel
    #11  No supported JavaScript runtime      -> --js-runtimes
    #12  Ignoring "deno,node" (lista)         -> a flag SE REPETE
    #13  solver OK, runtime OK                -> resta o IP

**MEDIDO com `-F` (listagem, sem baixar), contra `Jh9pFp1oM7E`:**

    player_client=android + cookie          ->   0 formatos
    player_client=android + cookie + ejs    ->   0 formatos
    padrao + cookie, sem ejs                ->   so' os 4 storyboard
    padrao + ejs, SEM cookie                ->  40+ formatos, ate' 4K

⚠️ **`The page needs to be reloaded`, `Requested format is not available` e
`Sign in to confirm you're not a bot` são O MESMO DEFEITO** visto de ângulos
diferentes. O projeto perseguiu três causas separadas por causa disso.

**Consertado** em `engine/midia.py`: `player_client=android` REMOVIDO (devolve
zero em qualquer combinação — o YouTube matou esse cliente), mais
`--remote-components ejs:github` e `--js-runtimes deno` + `--js-runtimes node`
(repetida, não lista). Guardado por `teste/teste_download_youtube.py`.

⚠️ **O cookie NÃO era necessário** — sem cookie e sem PO Token, só com o ejs,
saem 40+ formatos. Mas isso foi medido NESTA máquina, que não é IP de
datacenter. **Na nuvem o bot-check continua**, e é o único obstáculo que
resta lá.

⚠️ **O `cortar.yml` NUNCA funcionou** — 8 runs desde 28/07, todos falha. Eu
disse "quebrou em 05/09" e estava errado; olhei só os dois últimos. Quem
baixa de verdade é a **VPS do Bryan**, que este repositório não conhece.
`cortar_de_bruto.yml` e `repor_fila.yml` NÃO baixam do YouTube — trabalham
sobre bruto já no Drive, e por isso continuam verdes.

⚠️ **O secret `YTDLP_COOKIES` NÃO EXISTE** (conferido nos 62 secrets). O
bot-check de 05/09 foi lido como cookie vencido; nunca houve cookie.

---

## 4. A SENTINELA DO YOUTUBE — `engine/sentinela_youtube.py` (NOVA)

Ordem do Bryan depois de a VPS levar bot-check por rajada: **nunca mais de
uma chamada consecutiva ou junta, em QUALQUER projeto**, e quem chega dentro
do intervalo entra numa FILA em vez de ser recusado.

    UMA de cada vez    cadeado O_EXCL (atomico ate' em disco de rede)
    PESADO  video                  600s
    LEVE    legenda/metadado/-F    300s   <- e tem PREFERENCIA na fila
    FILA, nao recusa   quem chega cedo dorme e passa
    TETO                12 chamadas/dia (UTC)
    FREIO               bot-check -> trava TUDO por 24h e NAO tenta de novo
    FALHA FECHADA       sem cadeado, nao passa

    python -m engine.sentinela_youtube -- yt-dlp -f best URL
    python -m engine.sentinela_youtube --estado
    python -m engine.sentinela_youtube --soltar

Já **LIGADA** em `engine/midia.baixar()`: espera a vez antes do yt-dlp, e se
vier bot-check PUXA O FREIO no ponto que vê o erro e relevanta.

⚠️ **ONDE ELA NÃO PROTEGE, e está escrito no código:**
- **na nuvem é inócua** — cada run nasce em disco limpo, sempre vê "primeira
  chamada", e o freio não sobrevive entre runs;
- **na VPS ainda NÃO está ligada** — e é lá que o estouro aconteceu;
- **entre máquinas** só governa com disco de verdade compartilhado. Pasta do
  Drive NÃO serve: a sincronização demora e as duas leem "livre" ao mesmo
  tempo.

**O desenho que fecha isso: uma máquina só fala com o YouTube, as outras
pedem a ela.** A sentinela é a porta dessa máquina — falta escolher qual é.

⚠️ Os intervalos NÃO são medidos. São folgados de propósito: a operação
precisa de 2-4 vídeos/dia (cada fonte vira ~8 clipes, ~11 posts/dia).

---

## 5. MOTOR — o que mudou hoje

- **punch-in 6,5s -> 3,0s** e **card de título 3,5s -> 2,0s** (`render.py`),
  por ordem do Bryan. ⚠️ São GLOBAIS e foram ligadas JUNTAS: **a atribuição
  no próximo export está perdida, e ele decidiu ciente disso.** As duas
  entradas estão em `estado/calibragens.jsonl` com o `cai_se`: se a retenção
  agregada pós-09/09 não subir, caem juntas e voltam juntas.
- ⚠️ **A §4 do handoff de 08/09 apontava o arquivo ERRADO para a tarja.** O
  `0.30` do `engine/legendas.py` é o `LEGENDA_MARGEM_V_FRAC` — a margem que
  SOBE a legenda acima dos botões do TikTok, elevada em 28/07 por medição.
  Mexer nele **esconde a legenda** e não encolhe tarja nenhuma. O aviso ficou
  no próprio `render.py`.
- **FASE2 §2.2 RESOLVIDA**: o produto de afiliado vai no MANIFESTO, campo
  aninhado `produto`, ausente nos cinco canais de hoje. `engine/produto.py`
  valida (preço é TEXTO com `preco_em`; link só http(s), falha FECHADA;
  meio produto LEVANTA). `produto.linha_da_lista()` devolve a linha pronta
  para o grupo do WhatsApp.
- **7 canais no registro** (eram 5). `@achadinhos.instantaneos` e
  `@fatura.chora` existiam no TikTok e o motor não sabia. Entraram com
  `motor=False` e **campos do Buffer VAZIOS** — id inventado publicaria pelo
  token errado em silêncio. `teste_canal_sem_buffer.py` guarda.

---

## 6. CREDENCIAIS

- **Gemini: disco 15 -> 19, nuvem 27 -> 31.** As 4 recuperadas: 1 de
  `Cozinha Importada/motor/.env`, 3 de `Documents/ia mind/Apis.txt`.
  Órfãs (só na nuvem) caem de 12 para **8**.
- ⚠️ **A numeração do DISCO e da NUVEM DIVERGEM de propósito.** Disco
  `_16.._19` == nuvem `_28.._31`. A nuvem usa DOIS esquemas de nome
  (`GEMINI4` antigo e `GEMINI_API_KEY_20` novo) e os slots 1-27 estavam
  todos ocupados. **Comparar contagem por NOME dá conclusão errada.**
  `cadastrar_secrets_gemini.py` (novo) aborta se o destino já existir.
- ⚠️ **`Documents\ia mind\Apis.txt` é um despejo de credenciais em texto
  puro**, 192 linhas, com pelo menos um e-mail + senha. Parte dele passou
  pelo histórico de uma sessão. **Rotacionar.** E as 3 chaves Gemini de lá
  morrem junto quando isso acontecer.
- ⚠️ **O repositório é PÚBLICO** (`visibility: public`, conferido pela API).
  Sem segredo no git — mas o playbook, o `FASE2.md` e todo o estado estão
  no ar para qualquer um.

---

## 7. ACHADINHO — comunidade no WhatsApp (fora do git)

`Desktop\Tiktok\BACKUP_SISTEMA\SEGREDOS_NAO_SUBIR\CONVITES_WHATSAPP.md`

    Achadinho do Dia   bio dos canais sem grupo proprio
    Achadinho Make     @achadinho.make
    Achadinho Chef     @cozinha.internacional
    Achadinho Total    @achadinhos.instantaneos + @fatura.chora (MESMO convite)

Os 5 convites estão salvos lá, **e não no repo, porque ele é público**.

⚠️ Pendente do Bryan: apagar o grupo `Geral` (sobra automática do WhatsApp,
compete com o `Achadinho Total`), e decidir se quer convite separado para
`fatura.chora` — hoje os dois compartilham, e a origem não se separa.

⚠️ **`Achadinho Chef` já é o nome de exibição da cozinha.** O `@` vira
`@achadinho.chef` em **26/09** (carência do TikTok). São TRÊS lugares, e
estão escritos dentro do `engine/canais_registro.py`, no ponto do código.
**O `nome_buffer` NÃO muda.**

---

## 8. PENDENTE

**[BRYAN]**
1. **`crontab -l` e `systemctl list-timers` da VPS** — sem isso a sentinela
   fica ligada só onde o problema nunca esteve. É o item que destrava tudo.
2. **Cookie de conta DESCARTÁVEL na VPS** (nunca a principal). Testar com
   `yt-dlp --cookies ~/cookies.txt --remote-components ejs:github
   --js-runtimes deno -F "URL"` — o `-F` lista e não baixa.
3. **Rotacionar o que está no `Apis.txt`** e criptografar a pasta de
   segredos (7-Zip AES-256) — vem do handoff de 08/09.
4. Seguidores por vídeo no Studio · retenção por segundo · conta na Braip ·
   verificação de direitos autorais da música (todos de 08/09).
5. Apagar o grupo `Geral` da comunidade.

**[EU, quando houver o item 1]**
6. Ligar a sentinela nos disparadores da VPS.
7. Subir o servidor bgutil de verdade no workflow (hoje ele compila e não
   roda — é a única carta grátis que resta para a nuvem sem cookie).
8. Cauda muda no fim do clipe: quando a narração é mais curta, o vídeo roda
   sem voz até o fim. Terceira mudança de retenção, deixada de fora hoje
   para não cegar o mês.
9. Destilar os 4 vídeos do YouTube já separados (falta baixar os `.vtt`).

---

## 9. O QUE NÃO FAZER

Tudo do handoff de 08/09, mais:

- ⛔ **NUNCA dois downloads do YouTube ao mesmo tempo.** Palavras do Bryan:
  "NUNCA NUNCA NUNCA SIMULTANEO, ISSO FOI UM ERRO GRAVISSIMO". Um forno por
  vez, espaçado por HORAS. **Medir o que está em voo ANTES de qualquer
  disparo manual** (`actions/runs?status=in_progress` e `?status=queued`).
  Ver a §8 do `PIPELINE.md`.
- ⛔ **Nada de cookie do YouTube da conta principal.** Descartável, sim.
- **Não retentar rápido depois de bot-check** — tentar é o que confirma o
  padrão de robô.
- **Não citar "duplicata derruba o alcance" nem o rótulo de IA** como fato
  medido (vem de 08/09).
- **Não reintroduzir `player_client=android`** — o teste acusa, e ele
  devolve zero formatos.
- **Não mexer no slot das 16:27** (melhor medido, 1,50x).

---

## 10. FECHAMENTO

Sessão longa, com muito conserto e uma medição que muda a estratégia. O teste
`PRE_REGISTRO_TESTE_FONTE_01.md` (fonte escolhida por ENGAJAMENTO em vez de
views — o radar hoje ranqueia por views, e views não prevê conversão)
**continua NÃO REALIZADO**: os quatro runs morreram no download, nenhum byte
baixado, nada entrou na fila do Buffer.

**Nada foi postado, apagado nem devolvido à fila nesta sessão.**

⚠️ Padrão meu que se repetiu e vale para quem pegar: **três vezes eu provei
o caminho que eu controlava em vez do que ia rodar** — o `--js-runtimes`
testado sozinho e mandado em lista; a contagem de chaves feita só em
`Desktop\Tiktok` quando faltava o `ia mind`; e o "quebrou em 05/09" tirado
dos dois últimos runs quando havia oito.
