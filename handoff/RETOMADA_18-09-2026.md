# RETOMADA — 18/09/2026 (escrito 17/09 ~05:10 BRT)

Sessão de 16/09 20:30 → 17/09 05:10 BRT, contínua. **~20 commits.** Este é o que se lê primeiro;
o anterior é [`RETOMADA_17-09-2026.md`](RETOMADA_17-09-2026.md).

---

## ESTADO NO FECHAMENTO

```
local = remoto     main, 0 a 0 (último: fallback www -> raiz em JS)
DOMÍNIO            achadinhototal.com.br NO AR (registro.br -> Cloudflare NS carl/joyce,
                   zona active, CNAME @ e www -> achadinhototal.pages.dev, cert emitido).
                   Marca do domínio == marca do pages.dev (5346b55fac4d). http -> https 301.
                   www responde 200 + redirect em JS (o _redirects por hostname NÃO aplicou).
Search Console     propriedade "Domínio" VERIFICADA (TXT na zona). ⚠️ sitemap enviado errado
                   (a raiz, "está em HTML") — falta enviar `sitemap.xml`.
Site               régua 35/30/20/15 na ordem/fogo/ML; selos Espere (45), Recorde (0, série
                   de 5 dias), "a loja diz" (0, externas com 1 dia), "aqui R$ x mais barato"
                   (0 pares), "avise-me" (botão no ar, bot bryan_fxv_fila_bot, 0 inscrições);
                   canonical/OG, índice estático (142 Ali+ML), robots, sitemap.
Awin               série recebe o feed INTEIRO (26.455; 18.362 acima de R$150 antes sem
                   histórico); comissão real por loja (Kabum 1,15 — era 7,5 pra todas);
                   clickref=site-<id> em todo link (EPC por produto no relatório).
Nuvem              garimpo.yml grava awin_comissoes.json; precos.yml (24x/dia) roda
                   `engine.alertas --colher --avisar` com TELEGRAM_BOT_TOKEN.
Local              publicação diária S4U continua TRAVANDO no wrangler (não toquei);
                   publiquei à mão 3x nesta sessão (22 endereços conferidos).
Suíte              110 verdes; teste_vitrine_com_cartaz já falhava no HEAD de 16/09 (dado real).
```

Sujos que NÃO se commitam: `radar_modofuturo.json`, `estado/baixados_em_intervalos.json`,
`estado/videos_trabalhados.json`, `relato_*.txt`, `paginas/avatares/logo_app_aliexpress_100.png`.

---

## DECISÕES DO BRYAN NESTA SESSÃO (ordem)

1. **Coletar tudo**: série recebe todo produto, mesmo o que não entra na loja.
2. **Lojas externas = "só o ouro do ouro"**: fora do repo `bio` e do índice do Google.
3. **www → raiz**; Search Console verificado por ele.
4. **Régua 35/30** (Rende antes de Confiança). Doc: `CRITERIOS_DA_VITRINE.md`.
5. **5 selos aprovados** (1 comparação entre lojas, 2 avise-me, 3 "a loja diz", 4 espere,
   5 Recorde adquirido com ≥14 dias). Detalhe na memória `modofuturo-regua-da-vitrine-e-selos`.
6. **Bot do avise-me = o da operação** (`TELEGRAM_BOT_TOKEN`); ressalva: o username
   `bryan_fxv_fila_bot` aparece pro cliente; bot novo = trocar `TELEGRAM_BOT_ALERTA`.
7. Publicação feita à mão ("pode lançar na próxima rodada").

## O QUE ENTROU (commits, na ordem)

```
a425383  vendedores do ML na série/cartão · topo coerente · buscas --rotina --enviar na
         publicação diária · símbolos das lojas (mecanismo; falta o kit)
e44de5d  canonical/OG · índice estático · robots · sitemap · _redirects
1d23174  loja externa baixa também pelo menu Loja (Clovis 5983 no menu, 0 na grade)
be921b9  série recebe o feed Awin inteiro (teto só pro site)
e4c5b79  CRITERIOS_DA_VITRINE.md (429 fichas dos Maestros -> régua)
62fb9fc  engine/regua_vitrine.py: nota manda na ordem, fogo e ML (medido: topo 7/8 mudou;
         2 calibrações: volume na Confiança, x0,7 acima de R$150)
319c60d  clickref=site-<id> nos links Awin
6a52058  selos Espere e Recorde
2b25b10  comissão real por loja (API commissiongroups) -> estado/awin_comissoes.json
e21b9d0  selo 3 "a loja diz de X · nunca vimos acima de Y em N dias" (de_loja na série)
a7688e2  selo 1 pares entre lojas (duplicata.pares_entre_lojas, mesma foto)
2ea9763  selo 2 avise-me (engine/alertas.py + botão + passo horário no precos.yml)
c0181d0  reputação da loja (Reclame Aqui, MANUAL com data, 60 dias) -> Confiança externa
8e44956  alertas caem no TELEGRAM_BOT_TOKEN
+ head duplicado removido · fallback www em JS
```

⚠️ `engine/vitrine.py` é o POSTEIRO do canal (existia); a régua é `engine/regua_vitrine.py`.
Eu sobrescrevi o vitrine.py por engano e restaurei do git no mesmo minuto — conferido.

---

## 🔴 O QUE O BRYAN DECIDE / FAZ

1. **Search Console → Sitemaps**: remover a entrada da raiz; enviar `sitemap.xml`. Depois
   Inspeção de URL → `https://achadinhototal.com.br/` → Solicitar indexação.
2. **Cloudflare → Rules → Redirect Rules → template "WWW to root"** (1 clique). Hoje o
   www funciona por JS; a regra dá o 301 de verdade. Meu token não cria regra.
3. **Reclame Aqui**: ler no seu navegador a nota das 10 lojas (Nike, Kabum, Clovis, Lauri,
   Carraro, Leveros, Shark, Radiale, Exypna, Lacoste) e me mandar → gravo em
   `estado/reputacao_lojas.json` com data. O site bloqueia leitura automática.
4. **Símbolos das lojas**: PNG/SVG dos kits de afiliado → `paginas/simbolos_lojas/<Loja>.png`.
5. Bot próprio pro avise-me? (opcional — o atual funciona, só o nome é feio).
6. Fogo abre a página? / Kabum fica? — ainda abertas do handoff anterior; a régua já
   posiciona o Kabum sozinha (Rende baixo), então "fica" custa nada.

## 🟠 O QUE EU FAÇO NA PRÓXIMA SESSÃO

1. **Publicação diária S4U** (wrangler trava) — item 1 do handoff anterior, intocado.
2. **Garimpo do ML espelhando os campeões do Ali** (buscar pelo nome) — sem isso o selo 1
   fica em 0 pares.
3. Views do `desempenho` no eixo Mostrável (cruzar por id do produto).
4. Corte diário dos 20 % piores e furador de fila da `busca` (doc §2).
5. Conferir na 1ª rodada horária: `precos.yml` com o passo de alertas verde; na 1ª rodada
   do garimpo: `awin_comissoes.json` e `de_loja` na série; em 19/09 o selo 3 nascendo.
6. Redirect do pages.dev → domínio só DEPOIS de `conferir_no_ar` ler a marca no domínio.

## ⛔ ERROS MEUS DE HOJE

- **Sobrescrevi `engine/vitrine.py`** (Write em arquivo existente sem ler) — restaurado.
- **Patch aplicado 2×** no todos.html (o script estourou depois de escrever): bloco de head
  duplicado foi ao ar antes de eu ver (canonical 2×). Conserto: `s.count(old) == 1` ANTES
  de qualquer write, e um write só por script.
- `os.chdir` + caminho relativo gravou 5 JSONs na raiz do repo (removidos antes do commit).
- `taskkill` com filtro largo (não matou nada — conferido — mas era pra não existir).
- Heredoc comeu escapes 2× (de novo): arquivo com `\n` vai por Write/Edit, sempre.
- Régua v1 sem volume: topo virou "os mais caros" — medir o topo ANTES de commitar a
  fórmula, não depois.
