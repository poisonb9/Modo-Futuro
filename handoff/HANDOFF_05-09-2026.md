# HANDOFF — 05/09/2026

Continua o `handoff/HANDOFF_04-09-2026.md`, que cobre so' ate' as 23:40 UTC
de ontem. Este cobre as ~13h seguintes, em que o pipeline saiu de "dois
canais secando" para "tres canais no teto".

Leia tambem: `PIPELINE.md` (mapa), `FASE2.md` (achadinhos) e
`PRODUTO_DIGITAL.md` (novo, escrito hoje).

---

## 1. ESTADO MEDIDO (05/09, 12:26 UTC)

    @semanestesia.pod  10  (+55h)     @truque.importado   9  (+50h)
    @atefalhar          9  (+50h)     @modofuturo         8  (+47h)
    @cozinha.internac.  0  VAZIA
    TOTAL 36 posts

    fila de cortes: 25 pronto · 3 pendente · 3 disparado · 3 desistido
                    · 1 sem_fonte   (35 itens)

⚠️ **O gargalo inverteu.** Ha' 25 clipes prontos e a fila do Buffer comporta
10 por canal — a producao agora excede a postagem. Pela primeira vez na
sessao o problema nao e' produzir.

Comparado com ontem 22:26: 32 -> 36 posts, e o @truque saiu de 4 (+16h) e o
@semanestesia de 4 (+21h).

---

## 2. CONSERTOS DE HOJE (todos com suite verde, todos provados em producao)

    2860b02  canal nunca se adivinha: registro unico, recusa em vez de palpite
    868c0a0  janela != recorte (o defeito do Groq)
    3520dea  sonda de cota ANTES do download
    51e14c4  uma tabela de canais so': as 5 copias derivam do registro
    4ba28ad  a guarda de cota cobria UM caminho; ha' dois
    edfd66a  PIPELINE secao 5: nao ha' mais canal em estreia, e o piso engana

**A janela, provada:** run 33929995657 — `janela 300.0s->1500.0s`, 2 momentos
escolhidos, Groq transcrevendo **3,0 MB e 2,5 MB** contra os 36,8 MB que
matavam o run. Success em 54,3 min, notas 96 e 92. As 15 janelas do
@semanestesia foram todas reacendidas e produziram.

**A guarda de cota, provada nos dois lados:** abortou em segundos quando a
cota secou (run 33935432318) e deixou passar quando havia cota.

---

## 3. ⚠️ O ERRO MAIS CARO DO DIA FOI MEU

Disparei QUATRO runs no `cortar.yml` (o caminho do YouTube) sem notar que ele
**nunca teve um `success` na vida** — nem em julho. A causa esta' escrita na
primeira linha do `enviar_bruto_drive.py` desde 28/07:

    "o download continua sendo feito aqui, local, porque IP de datacenter
     e' bloqueado pelo YouTube"

A primeira falha veio com uma explicacao confortavel ("foi cota", e era
mesmo) e eu parei de investigar ali. **Explicacao plausivel nao e' causa
medida.** O sinal estava a` vista: workflow sem nenhum sucesso no historico.

⚠️ **Nao dispare `cortar.yml`.** O caminho que funciona e': baixar aqui com
yt-dlp -> `enviar_bruto_drive.py` -> vigia -> `cortar_de_bruto.yml`.

---

## 4. ⚠️ E O VIGIA SO' DESCE 3 NIVEIS

Ao subir os dois videos do truque, o `enviar_bruto_drive.py` criou
`brutos/<data>/` por conta propria. O arquivo ficou no NIVEL 4:

    RAW / Truque Importado / brutos / 2026-09-05 / arquivo

O vigia tem `profundidade_max=3`. Resultado: cinco passadas dizendo "nada
novo", sem erro nenhum — o tipo de falha que o proprio docstring do
`videos_em_raw` chama de "a pior: silenciosa". Movi os arquivos um andar
acima e o vigia enxergou na hora.

⚠️ **CONSERTO PENDENTE, ainda nao autorizado:** fazer o vigia AVISAR quando
encontrar pasta no ultimo nivel que ele consegue descer. Transforma silencio
em aviso, que e' a regra da secao 9 do PIPELINE.

---

## 5. TRES VEZES UM TESTE VERDE PROTEGEU O DEFEITO

  - `teste_vigia_manda_canal` EXIGIA `cozinha.internacional` — o nome que
    fazia o token cair no `else` e sair como modofuturo;
  - `teste_fila_passa_recorte` dizia na docstring que "recorte e' a janela" —
    o mal-entendido inteiro, fixado em verde;
  - a fusao das tabelas quebrou o painel (`? sem token` na cozinha) e os 53
    testes passaram. So' apareceu ao rodar `painel_filas.py` de verdade.

**O padrao:** teste que fixa a GRAFIA de um acordo entre dois modulos congela
o mal-entendido. E suite verde nao substitui rodar o comando.

---

## 6. COZINHA: consertada por dentro, parada por fora

O conserto do "220°C Fahrenheit" **esta' no ar** (`24d5e06` + `f5d0b86` no
`bryanaw2121-sketch/pipeline`), com a suite de la' em 12/12 e o
comportamento conferido no codigo de producao:

    'Bake at 425 degrees Fahrenheit or 220 Celsius.' -> 'Bake at 220°C.'
    'cook at 220 degrees Celsius'                    -> 'cook at 220°C'  (nao 104)

⚠️ **E agora ha' acesso de escrita la'**: deploy key
`SHA256:nPJoUdA7vyXUKMeEhNqq44/L+FRu+0yKdQBy4lvjpQQ`, privada em
`C:\Users\Administrator\.ssh\pipeline_cozinha_ed25519`, alcance de UM
repositorio. O `push` do clone local vai por SSH; o `fetch` continua HTTPS.

⚠️ O campo `permissions` da API MENTIU de novo: disse `push: True` para o
token enquanto o `git push` dava 403. A conta tinha Write; o PAT
fine-grained nao alcanca repo de outro dono. So' o push a seco prova.

**Mas a fila do canal esta' em 0, e sao TRES causas, nenhuma o Fahrenheit:**

  1. fila de cortes esgotada — 4 itens, ZERO `pendente`;
  2. 2 itens presos em `disparado` ha' dezenas de passadas;
  3. **aquele repo nao tem `repor_fila`** — nada reabastece a fila do Buffer.

⚠️ Nao mexi. Escrever la' precisa do ok do Bryan (perguntado 3x, sem resposta).

---

## 7. FONTES E RADAR

**Truque:** baixei e subi 2 fontes (Tina Yong, kelly) — o canal saiu de 4 pra
10 posts. O caminho local->RAW funciona e **eu consigo fazer sozinho**;
corrija o handoff de ontem, que dizia ser tarefa exclusiva do Bryan.

**Radar de maquiagem coreana (05/09):** o Hyunjin que bombou veio de
`9wjRjp6QpfQ` (RISABAE, 6,39M views) e e' o UNICO dele com ela. O veio
vizinho e' grande — a RISABAE tem uma serie de maquiagem em idolo de K-pop,
0,7 a 6M views, 13 a 26 min, nenhuma cortada. O melhor candidato e' o Felix
(mesmo grupo, mesma criadora, 5,97M): `tdrNkhv5Hws`. Lista completa em
`scratchpad/candidatos_maquiagem_coreana.txt` (efemera — reproduzivel com
yt-dlp `ytsearch`).

⚠️ **Nao ha' como conferir duplicata de fonte**: `url_origem` e' None em 175
de 175 clipes do manifesto. A unica rede e' o sha256, que age DEPOIS do
runner gasto. Conferi os candidatos a olho.

⚠️ **E o desempenho nao esta' medido**: as 42 leituras do `desempenho.jsonl`
estao TODAS com 0 views — a dessincronia do Buffer. Quando o Bryan diz "o
Hyunjin bombou", ele olhou o TikTok, que e' a fonte boa.

---

## 8. PENDENTE

  1. **[BRYAN] Criar conta na Braip** — trava o levantamento de demanda do
     `PRODUTO_DIGITAL.md`.
  2. **[BRYAN] Autorizar** escrita no repo da cozinha (secao 6).
  3. **[BRYAN] Escolher** quais fontes coreanas baixar (secao 7).
  4. **[EU] Conserto do vigia** — avisar em vez de silenciar (secao 4).
  5. **[EU] `url_origem` no manifesto** — fechar o buraco de duplicata.
  6. Fase 2 (achadinhos) e produto digital: os dois documentos existem e os
     bloqueadores de codigo ja' cairam.

⚠️ O loop de hora em hora foi ENCERRADO (job 9affe23d) apos quatro ciclos
identicos. Retomar com `/loop 1h <tarefa>`.

⚠️ **Eu nao rodo entre turnos.**
